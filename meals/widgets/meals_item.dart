import 'package:flutter/material.dart';

class MealItem extends  extends StatelessWidget {
  const MealItem({super.key,required this.meal,});
  final Meal meal;
  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}